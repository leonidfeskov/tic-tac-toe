const initialState = {
    data: [
        [null, null, null],
        [null, null, null],
        [null, null, null]
    ]
};

export default function field(state = initialState) {
    return state;
}